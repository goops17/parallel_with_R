library(MASS)
# r = range(painters$Expression)
# bins <- seq(0,18,by=3)
# #intervals <- cut(painters$Expression, bins, right=FALSE)
# print("Frequency distribution is:" )
# print(table(intervals))
# print ("Highest frequency and experession associated are: ")
# print (sort(unlist(table(intervals)), decreasing = TRUE)[1])
# cat("\n\n" )
# print(cbind(table(intervals)))
# colors = c("red", "yellow", "green", "violet","orange", "blue", "pink", "cyan") 
# barplot(table(intervals), col=colors)
expression = painters$Expression
fq = expression.freq = table(expression)
print(fq)
cat("\n Highest expression and frequency are: ")
print(sort(unlist(fq), decreasing = TRUE)[1])
cat("\n")
print(cbind(fq))
colors = c("red", "yellow", "green", "violet","orange", "blue", "pink", "cyan") 
barplot(fq, col=colors)